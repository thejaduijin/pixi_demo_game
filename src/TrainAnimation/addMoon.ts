// import { Assets, Graphics, Sprite } from 'pixi.js';
// import moonSvg  from ""


// export function addMoon(app: any) {
// Create a moon graphics object from an SVG code.
// const graphics = new Graphics().svg(moonSvg);

// Load the SVG file as a texture

// const moonSvg = Assets.load('./moon.svg');
// moonSvg.then((textureResolved) => {
//     let moon = Sprite.from(textureResolved)
//     moon.x = app.screen.width / 2 + 100;
//     moon.y = app.screen.height / 8;
//     app.stage.addChild(moon);
// })
// app.loader.add('myIcon', './moon.svg').load((loader: any, resources: any) => {
//     const sprite = new Sprite(resources.myIcon.texture);
//     console.log("sprite",sprite)
//     // Position the moon.
//     sprite.x = app.screen.width / 2 + 100;
//     sprite.y = app.screen.height / 8;
//     app.stage.addChild(sprite); // Add sprite to the stage
// });
// }
